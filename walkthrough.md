# 🌌 고등 물리 & 수학 융합 웹 애플리케이션 개발 완료 보고서

고등학생을 위한 물리학(자유낙하 및 포물선 운동)과 수학(이차함수 $y = ax^2 + bx$) 융합 교육용 인터랙티브 웹 시뮬레이터 개발이 완료되었습니다.

---

## 1. 구현된 핵심 기능 요약

### ① 상단 진공 챔버 자유낙하 무한 반복 실험
- **질량 무관성 시각화**: 깃털(10g)과 쇠구슬(5,000g)이 공기 저항이 없는 진공 챔버에서 완벽히 동일한 가속도($g=9.8\text{ m/s}^2$)와 속도로 동시에 낙하하는 무한 루프 애니메이션 제공
- **제어 기능**: 슬로우 모션 전환(0.35x), 다시 시작 기능 지원

### ② 유연한 파라미터 제어 및 다중 투사체 질량 비교 실험 (사용자 요구사항 반영)
- **공통 발사각 ($\theta$) & 공통 초기속도 ($v_0$) 적용**:
  - 모든 투사체가 **동일한 발사각과 초기속도**를 공통으로 사용하도록 통일하였습니다.
  - 상단 컨트롤 패널에서 발사각($5^\circ \sim 85^\circ$)과 초기속도($5 \sim 60\text{ m/s}$)를 조절하면 모든 투사체에 실시간 동기화됩니다.
- **투사체 수(1~5개) 및 개별 질량($m$) 설정**:
  - 사용자가 원하는 개수만큼 투사체를 추가하고, 각 투사체별 질량($10\text{ g} \sim 10,000\text{ g}$)을 다르게 설정하여 실험할 수 있습니다.
  - 질량이 서로 다른 투사체들이 **완전히 동일한 궤적과 비행 시간**으로 동시에 착지함을 명확히 증명합니다.

### ③ 실시간 캔버스 시뮬레이션 및 데이터 분석
- **물리 렌더링**: 잔상(Trail) 효과, 실시간 속도 벡터($v_x, v_y, v$), 최고점(H) 마커, 도달거리(R) 마커 표시
- **재생 컨트롤**: 발사, 일시정지, 초기화, 배속 조절(0.25x ~ 2.0x)
- **실시간 결과 테이블**: 각 투사체별 최고점 높이, 도달거리, 체공시간 및 궤적 일치 여부 뱃지 출력

### ④ 수학적 분석 및 인터랙티브 이차함수 그래프
- **수식 유도 및 증명**: $x(t)$와 $y(t)$로부터 시간 $t$를 소거하여 $y = ax^2 + bx$ 형태의 이차함수가 유도되는 과정을 고등학생 눈높이에 맞춰 KaTeX 수식 카드로 제시
- **이차함수 데카르트 그래프 (Chart.js)**: 투사체별 궤적을 수학 좌표계로 렌더링하고, 유도된 실제 이차함수 식 및 꼭짓점 좌표 표기

---

## 2. 브라우저 테스트 및 검증 결과

실제 브라우저 환경에서 시뮬레이션을 실행하여 모든 기능이 정상 작동함을 확인하였습니다.

- **자유낙하 챔버**: 체공시간 0.70s, 낙하 속도 6.9 m/s로 깃털과 쇠구슬 동시 착지 검증
- **질량 비교 실험**: 10g 깃털과 5000g 쇠구슬의 포물선 비행 궤적 및 체공시간이 100% 일치함 확인
- **이차함수 계수 일치성**: 
  - $45^\circ, 25\text{ m/s} \rightarrow y = -0.0157x^2 + 1.000x$ (최고점 15.9m, x절편 63.8m)
  - $60^\circ, 25\text{ m/s} \rightarrow y = -0.0314x^2 + 1.732x$ (최고점 23.9m, x절편 55.2m)
  - 이론 계산값과 캔버스/그래프 상의 결과가 완벽히 일치함을 확인

---

## 3. Render 배포 안내

이 프로젝트는 **정적 웹(Static Site)** 형식으로 개발되어 Render에 무료로 배포하고 웹 링크를 고객(학생)에게 공유할 수 있습니다.

1. 본 소스 코드를 GitHub 저장소에 업로드합니다.
2. [Render 대시보드](https://dashboard.render.com/)에서 **`New +` > `Static Site`** 를 클릭합니다.
3. GitHub 저장소를 선택한 뒤, **Build Command**는 비워두고 **Publish Directory**는 `./`로 지정합니다.
4. 배포가 완료되면 생성된 URL(예: `https://physics-lab-xxxx.onrender.com`)을 학생들에게 전달하시면 됩니다.
