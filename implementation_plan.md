# 자유낙하 및 포물선 운동 물리/수학 시뮬레이션 웹 애플리케이션 개발 계획

고등학생 대상의 물리학 및 수학 융합 교육용 웹 애플리케이션을 개발합니다.
"진공 상태에서 물체의 낙하 속도 및 궤적은 질량과 무관하다"는 물리학 개념과, 포물선 궤적이 **이차함수 $y = ax^2 + bx + c$** 형태를 띤다는 수학적 개념을 직관적인 애니메이션과 인터랙티브 그래프로 제공합니다.

---

## 1. 주요 특징 및 아키텍처

- **배포 및 서버**: Render 정적 웹 호스팅(Static Site) 또는 경량 Node/Python 서버 연동이 가능한 구조 (프론트엔드 중심의 독립 실행형 번들 + 배포 가이드 제공).
- **기술 스택**:
  - **HTML5 Canvas / 현대적 JavaScript (ES Modules)**: 부드럽고 정확한 물리 엔진 및 실시간 애니메이션 구현
  - **CSS3 / Glassmorphism UI**: 세련된 다크/라이트 하이테크 스타일, 직관적인 고등학생 친화형 UI
  - **Chart.js 또는 Custom Canvas Graph**: 이차함수 궤적 및 수식($y(t) = v_{0y}t - \frac{1}{2}gt^2$, $y(x) = \tan(\theta)x - \frac{g}{2v_0^2\cos^2\theta}x^2$) 실시간 렌더링
  - **KaTeX / MathJax**: 물리/수학 공식 실시간 표기 지원

---

## 2. 화면 구성 및 핵심 기능

### ① 상단 인트로: 자유낙하 운동 무한 반복 애니메이션
- **진공 챔버 시각화**: 깃털(가벼운 물체)과 볼링공/쇠구슬(무거운 물체)이 진공 속에서 동시에 낙하하는 무한 루프 애니메이션
- **핵심 설명 카드**: "공기 저항이 없는 진공에서는 질량에 관계없이 중력가속도($g = 9.8\text{ m/s}^2$)로 동일하게 가속됩니다." 명시

### ② 파라미터 입력 및 제어 영역
- **입력 항목**:
  - 발사 각도 ($\theta$, 단위: $^\circ$, $0^\circ \sim 90^\circ$)
  - 초기 속도 ($v_0$, 단위: $\text{m/s}$, $1 \sim 100\text{ m/s}$)
  - 질량 ($m$, 단위: $\text{g}$, $1 \sim 10000\text{ g}$)
- **비교 모드 (멀티 물체 발사)**: 서로 다른 질량(예: 10g 깃털 vs 5kg 쇠구슬)을 동시에 발사하여 질량이 궤적에 영향을 주지 않음을 눈으로 직접 증명하는 기능
- **실행, 일시정지, 리셋, 슬로우 모션(0.5x, 1x, 2x)** 컨트롤 지원

### ③ 메인 시뮬레이션 캔버스 (물리 애니메이션)
- 투사체의 실시간 포물선 비행 궤적 애니메이션
- 잔상(Trail) 및 실시간 벡터 화살표(속도 벡터 $v_x, v_y$, 중력가속도 $g$) 표시 옵션
- 최고점(Peak), 도달 거리(Range), 비행 시간(Flight Time) 실시간 마커

### ④ 수학적 분석 & 이차함수 그래프 영역
- **이차함수 유도 및 계수 분석**:
  - 시간-위치 관계식: $y(t) = -\frac{1}{2}gt^2 + v_0\sin(\theta)t$
  - 궤적 방정식 (이차함수 $y = ax^2 + bx$): $a = -\frac{g}{2(v_0\cos\theta)^2}$, $b = \tan\theta$
  - 꼭짓점(최고점) 좌표: $(x_p, y_p) = (-\frac{b}{2a}, -\frac{b^2}{4a})$
- **인터랙티브 그래프**: 수학 시간에 배우는 표준 좌표계와 매핑하여 고등학생이 직관적으로 이해할 수 있도록 구성

### ⑤ Render 배포 설정 파일
- `render.yaml` 및 정적 배포 세팅 안내 포함

---

## 3. 제안 변경 사항 (파일 목록)

```
d:/Projects/temp/
├── index.html              # 메인 애플리케이션 레이아웃
├── css/
│   └── style.css           # 모던 UI/UX 스타일링 (다크 테마 + 애니메이션)
├── js/
│   ├── physics.js          # 물리 및 수학 공식 계산 엔진
│   ├── introCanvas.js      # 상단 자유낙하 무한 루프 캔버스
│   ├── simulationCanvas.js # 메인 포물선 발사 시뮬레이터 캔버스
│   ├── mathGraph.js        # 이차함수 분석 그래프 렌더러
│   └── app.js              # 이벤트 핸들러 및 통합 컨트롤러
├── render.yaml             # Render 원클릭 배포 설정
├── package.json / server.js# (선택) 로컬 테스트 및 웹 호스팅용 초경량 서버
└── README.md               # 학생/교사용 가이드 및 Render 배포 안내서
```

---

## 4. 검증 계획

### 수동 검증 및 동작 테스트
1. 브라우저에서 상단 자유낙하 애니메이션(무게가 다른 두 물체의 동시 낙하)이 부드럽게 무한 루프되는지 확인.
2. 각도 $0^\circ \sim 90^\circ$, 속도, 질량 조절 후 발사 시 궤적과 이차함수 그래프가 정확히 일치하는지 수학적/물리학적 검증.
3. 질량을 10g에서 5000g으로 변경해도 궤적이 정확히 동일함을 비교 모드로 확인.
4. 로컬 서버 구동 및 브라우저 렌더링 확인.
